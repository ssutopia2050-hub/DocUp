<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="shortcut icon" href="/images/favicon-32x32.png" type="image/x-icon">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Archivo+Black&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Saira:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/signup_style.css">
    <meta charset="UTF-8">
    <title>DocUp |  Forgot Password</title>
</head>

<body>
<div class="box">
    <h1>DocUp</h1>

    <form method="post" action="/forgot_password">

        <p id="timer"
           style="display:none;color:#ffcc00;font-size:14px;border:1px solid #ffcc00;padding:10px 20px;border-radius:10px">
        </p>

        <input type="text" placeholder="Email Address" name="email" required>

        <% if(err){ %>
        <p style="color:#ffb100"><%= err.message %></p>
        <% } %>

        <button
                id="resendBtn"
                type="submit"
                formnovalidate>
            Send Password To Mail
        </button>

        <button type="button" onclick="handleback()">Sign In</button>

    </form>
    <p style="color: rgba(255,255,255,0.15)">You are currently on version 1.0.2 of DocUp</p>
</div>

<script>

    function handleback(){
        window.location.href="/signin";
    }

    const resendAllowedAt = Number("<%= typeof resendAllowedAt !== 'undefined' ? resendAllowedAt : 0 %>");
    const timerEl = document.getElementById("timer");
    const resendBtn = document.getElementById("resendBtn");

    function updateTimers() {

        const now = Date.now();
        const resendRemaining = Math.floor((resendAllowedAt - now) / 1000);

        if (resendRemaining > 0) {

            const mins = Math.floor(resendRemaining / 60);
            const secs = String(resendRemaining % 60).padStart(2,'0');

            resendBtn.disabled = true;
            resendBtn.innerText = "Resend Email (" + mins + "m " + secs + "s)";
            resendBtn.style.opacity = "0.6";
            resendBtn.style.cursor = "not-allowed";

            timerEl.style.display = "block";
            timerEl.innerText = "You can request another email in " + mins + "m " + secs + "s";

        } else {

            resendBtn.disabled = false;
            resendBtn.innerText = "Send Password To Mail";
            resendBtn.style.opacity = "1";
            resendBtn.style.cursor = "pointer";

            timerEl.style.display = "none";
        }
    }

    setInterval(updateTimers, 1000);
    updateTimers();

</script>

</body>
</html>